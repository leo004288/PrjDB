# PrjDB

## 드라이버 다운로드 경로
1. mvnrepository 검색 -> oracle11 -> Version 23.26.1.0.0 -> files : jar

2. sts Project : PrjDB 생성
   lib 폴더 생성 -> 폴더에 .jar 붙여넣기
 
3. sts PrjDB 우클릭 -> properties -> java build path -> libraies -> class path 
   -> add jar (PrjDB -> lib -> jar)

Referenced libraies 생성	